# Server
NODE_ENV=development
PORT=5000

# Database: PostgreSQL only. Set DATABASE_URL in Dokploy (backend app Environment).
# Example (this project):
DATABASE_URL=postgresql://user_iphone_center:center_iphone283901@iphone-center-database-ewasbn:5432/iphone-center-db
# If your password contains @ or # etc., URL-encode it (e.g. @ → %40, # → %23).
# Or use separate vars (same host/port/user/password/db as in the URL):
# DB_HOST=iphone-center-database-ewasbn
# DB_PORT=5432
# DB_USER=user_iphone_center
# DB_PASSWORD=center_iphone283901
# DB_NAME=iphone-center-db

# JWT - REQUIRED. Use long random strings in production (32+ chars).
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production

# CORS - allowed frontend origin(s). Comma-separated for multiple (no spaces after comma).
# Backend is https://backend.iphonecenter.lk - set this to your frontend URL:
CORS_ORIGIN=https://cloud.iphonecenter.lk

# Test/demo login (not stored in database). Use to log in and create real users in the panel.
# Default: test / test. Set to empty to disable.
TEST_LOGIN_USERNAME=test
TEST_LOGIN_PASSWORD=test

# Logging
LOG_LEVEL=info
