# Backend API base URL (no trailing slash). Required for login and API calls.
# Production (must match your backend domain):
VITE_API_URL=https://backend.iphonecenter.lk
# Local: VITE_API_URL=http://localhost:5000
