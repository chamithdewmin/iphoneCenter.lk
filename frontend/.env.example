# Backend API base URL (no trailing slash). Required for login and API calls.
# Production (frontend & API on same domain):
VITE_API_URL=https://iphonecenter.logozodev.com
# Local: VITE_API_URL=http://localhost:3001
