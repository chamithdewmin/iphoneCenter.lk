# Copy to .env and fill in. Required for docker-compose.

# MySQL
MYSQL_ROOT_PASSWORD=rootpassword
DB_NAME=pos_system
DB_USER=pos_user
DB_PASSWORD=pos_password

# JWT - REQUIRED. Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
JWT_REFRESH_SECRET=

# Frontend origin (for CORS). Use https in production.
CORS_ORIGIN=http://localhost:3000

# Optional: API URL baked into frontend build (e.g. https://api.yourdomain.com)
# VITE_API_URL=http://localhost:3001
